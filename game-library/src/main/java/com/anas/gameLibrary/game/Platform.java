package com.anas.gameLibrary.game;

/**
 * Enum representing supported gaming platforms.
 * Used to classify the platform on which a game can be played.
 */
public enum Platform {
    PC,
    PS5,
    XBOX,
    SWITCH,
    ANDROID,
    IOS
}
